#ifndef SORT_MERGE_H_
#define SORT_MERGE_H_

/**
 * @brief Sort an array with merge sort
 * @param[in, out] arr array to be sorted.
 * @param[in] arr_size size of the array.
 */
void Sort_merge(int *arr, int arr_size);

#endif /* SORT_MERGE_H_ */