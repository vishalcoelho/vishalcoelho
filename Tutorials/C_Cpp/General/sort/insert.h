#ifndef SORT_INSERT_H_
#define SORT_INSERT_H_

/**
 * @brief Sort an array with an insertion sort
 * @param[in, out] arr array to be sorted.
 * @param[in] arr_size size of the array.
 */
void Sort_insert(int *arr, int arr_size);

#endif /* SORT_INSERT_H_ */